# AI 角色工坊 · V2 升级计划

## V1 回顾（已交付）

| 能力 | 状态 |
|------|------|
| 10 模板 + 填空式创建 | ✅ |
| LLM 流式聊天 | ✅ |
| 大白话微调按钮 | ✅ |
| 聊天记录自动保存 | ✅ |
| SKILL.md / JSON 导出 | ✅ |
| 双击 `启动.bat` 即用 | ✅ |

## V1 已知问题

| 问题 | 影响 | 根因 |
|------|------|------|
| 贴聊天记录后角色不像 | 核心体验受损 | 一步生成 Prompt 质量不可控，聊天记录有双方对话 AI 分不清模仿谁 |
| 角色定义不结构化 | 导出/分享/微调困难 | System Prompt 是一坨文本 |
| 专业知识占 token | 聊几轮就超限 | 全部知识塞进 System Prompt |

---

## Phase 6：角色创建质量提升（优先做）

### 6.1 两步生成 System Prompt

**现在：** 贴聊天记录 → LLM 一步生成 System Prompt

**改后：**

```
Step 1：提取角色画像
  用户贴的聊天记录 → LLM 提取：
    - 语气特征（温柔/毒舌/傲娇...）
    - 口头禅（自动识别高频词）
    - 句式特点（短句/长句/反问/省略号）
    - 情感倾向（表面冷酷内心关心）

Step 2：生成示例对话
  根据画像 → LLM 生成 3-5 句该角色会说的话：
    "别人说：'今天加班好累'
     你回：'害，都是打工的命，来，抽根烟'"

Step 3：注入 System Prompt
  画像摘要 + 示例对话 → 结构化 System Prompt
```

**为什么有效：** LLM 看到示例对话比看到原始聊天记录有效 10 倍。原始记录是噪音，示例是压缩后的行为指令。

### 6.2 贴原话区域增加引导

```
如果贴的是和 ta 的聊天记录：
  ○ 左边是你，ta 是右边的人
  ○ 右边是你，ta 是左边的人
  ☑ 只贴了 ta 说的话（最理想！）

  我只贴了 ta 单边说的话（不用分析）
```

### 6.3 创建过程可视化

生成角色时不再只有一个转圈，而是：

```
🎨 正在分析 ta 的说话方式...  ✓
📝 正在生成示例对话...       ✓
🔧 正在构建角色...            ⏳
```

用户看到过程就有耐心等。

**文件：**
- 新增 `src/utils/voiceExtractor.ts` — 两步生成引擎
- 修改 `src/components/home/CreateWizard.tsx` — 替换一步生成为两步 + 引导文案

---

## Phase 7：角色卡标准化 + Prompt 分区

### 7.1 角色卡标准格式

把 `Character` 类型从扁平结构改为分区结构：

```
现在：
systemPrompt: "你是老王。东北退休工程师。说话带东北口音。爱用修东西比喻。..."

改后：
{
  identity: "你是老王，东北退休工程师，60岁",
  voice: "说话带东北口音，每句话结尾爱加'啊'、'呗'。最爱说'这玩意儿跟修水管一样...'",
  rules: "不要用网络用语。不要煽情。看到别人难过就用修理东西的比喻安慰。",
  samples: [
    { user: "我工作压力好大", reply: "嗨，这玩意儿跟修水管一样，你得先找到哪里漏水。来，坐，叔帮你看看。" },
    { user: "我涨工资了", reply: "好啊！有出息！不过低调点，树大招风啊。" },
  ],
  knowledge: "他擅长水电维修，懂得机械原理，当过 30 年工程师。",
}
```

### 7.2 聊天页可查看/编辑每个分区

微调弹窗不再是一个大白话输入框，而是分区卡片：

```
┌─ 微调角色 ────────────────────────┐
│                                     │
│  📛 身份：「老王」— 东北退休工程师   │
│  📝 语气：带口音，用比喻            │
│  🚫 规则：不煽情                   │
│  💬 示例：[3 句示例对话]            │
│  📚 知识：[专业知识摘要]            │
│                                     │
│ 每个卡片点开都能直接编辑 System     │
│ Prompt 原文，也可以点「大白话微调」 │
└─────────────────────────────────────┘
```

**文件：**
- 修改 `src/types/character.ts` — Character 类型改为分区结构
- 修改 `src/components/home/CreateWizard.tsx` — 适配新结构
- 修改 `src/components/chat/TweakDialog.tsx` — 分区展示
- 修改 `src/components/chat/PersonaCard.tsx` — 显示分区摘要
- 修改 `src/utils/skillExport.ts` — 导出新格式

---

## Phase 8：WorldInfo 知识书

### 核心思路

用户贴的 3000 字法律笔记不再全塞 System Prompt，而是：

```
1. LLM 自动提取关键词 → 知识条目
   "行政复议" → "行政复议是公民认为行政行为侵犯权益..."
   "行政诉讼" → "行政诉讼是向法院起诉行政机关..."

2. 聊天时，用户问："行政复议和行政诉讼有什么区别"
   → 系统检测到关键词 "行政复议" "行政诉讼"
   → 把这两个条目注入当前对话
   → 省掉无关条目，省 token
```

### 用户侧体验

创建角色时，专家模式的知识区域：

```
选了「需要专业知识」
  └─ 粘贴知识材料（法律笔记/医学常识/教材片段）
     └─ [自动提取关键知识点] ← 新增按钮
        └─ 显示提取结果：
           · 行政复议 → [预览]
           · 行政诉讼 → [预览]
           · 行政行为 → [预览]
           [每个都可以编辑/删除/添加]
```

聊天时完全无感，自动按需注入。用户不需要知道 WorldInfo 这个词。

**文件：**
- 新增 `src/components/chat/WorldInfoPanel.tsx` — 知识条目管理
- 新增 `src/utils/knowledgeExtractor.ts` — LLM 提取关键词
- 修改 `src/components/chat/ChatPanel.tsx` — 聊天时动态注入

---

## Phase 9：角色导入 + 头像上传

### 9.1 导入角色

首页新增「导入角色」按钮，支持：

- 拖入 JSON 文件 → 解析 → 加入角色列表
- 拖入 SillyTavern 兼容的角色卡 PNG → 解析 metadata → 加入角色列表

### 9.2 头像上传

创建角色时头像选择器：emoji 列表 + 上传图片按钮。图片以 base64 存 localStorage。

**文件：**
- 新增 `src/components/home/ImportDialog.tsx`
- 修改 `src/components/home/HomePage.tsx` — 添加导入按钮
- 修改 `src/components/home/CreateWizard.tsx` — 头像上传

---

## 执行优先级

```
P0 (立刻)：Phase 6 — 角色创建质量提升 ← 修复朋友反馈的问题
P1 (接下来)：Phase 7 — 角色卡标准化 ← 为后续所有功能打基础
P2 (后续)：Phase 8 — WorldInfo ← 差异化竞争力
P3 (收尾)：Phase 9 — 导入 + 头像 ← 锦上添花
```

---

## 不做的事

| 功能 | 理由 |
|------|------|
| 视觉小说模式 | 太重，受众是二次元玩家不是普通人 |
| TTS 语音 | 依赖浏览器 API，不是核心需求 |
| 群聊 | 复杂度高，普通用户用不上 |
| 插件系统 | 我们的受众不是开发者 |
| 本地模型 | 需要 Node.js 后端，破坏"双击即用" |

---

每个 Phase 独立提交，互不阻塞。先做 P0，要开始吗？
